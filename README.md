# Harshit Gupta — Portfolio (Plain HTML)

Drop this folder into a GitHub repository. Add `HarshitGuptaResume_1Aug2025.pdf` to the repo root.

To host on GitHub Pages:
- Option A: Create repo named `YOURUSERNAME.github.io` and push to `main`.
- Option B: Use any repo, then go to Settings → Pages → choose `main` branch and `/ (root)` or `/docs` folder.

Resume download link: `/HarshitGuptaResume_1Aug2025.pdf`
